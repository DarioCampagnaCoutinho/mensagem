version: "3.8"

services:
  web:
    build: .
    image: mensagem_web:latest
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:ro
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

